import React from 'react';

// TODO (Week 2): Display final score, accuracy, combo, judgement breakdown.
// TODO (Week 2): Add replay export button (useExport hook).
// TODO (Week 2): Add "Play Again" and "Song Select" navigation.

export default function ResultScreen(): React.ReactElement {
  return (
    <div style={{ color: '#fff', padding: 32 }}>
      <h1>Result</h1>
      <p>TODO (Week 2): Score summary + replay export</p>
    </div>
  );
}
